// ============================================================================
// PM2 Ecosystem Configuration Template
// ============================================================================
// Variables:
//   APP_NAME     - e.g., "my-portfolio"
//   APP_CWD      - e.g., "/var/www/projects/my-portfolio"
//   APP_PORT     - e.g., 4321
//   MAX_MEMORY   - e.g., "500M"
// ============================================================================

module.exports = {
  apps: [{
    name: '{{ APP_NAME }}',
    script: 'npm',
    args: 'start',
    cwd: '{{ APP_CWD }}',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '{{ MAX_MEMORY | default("500M") }}',
    env: {
      NODE_ENV: 'production',
      PORT: {{ APP_PORT | default(4321) }}
    },
    // Logging
    error_file: '{{ APP_CWD }}/logs/pm2-error.log',
    out_file: '{{ APP_CWD }}/logs/pm2-out.log',
    merge_logs: true,
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
    // Restart policy
    exp_backoff_restart_delay: 100,
    max_restarts: 10,
    min_uptime: '10s',
  }]
};
